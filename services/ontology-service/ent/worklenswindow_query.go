// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"cubicle/services/ontology-service/ent/documentlensresult"
	"cubicle/services/ontology-service/ent/messagelensresult"
	"cubicle/services/ontology-service/ent/predicate"
	"cubicle/services/ontology-service/ent/pullrequestlensresult"
	"cubicle/services/ontology-service/ent/ticketlensresult"
	"cubicle/services/ontology-service/ent/worklens"
	"cubicle/services/ontology-service/ent/worklenswindow"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// WorkLensWindowQuery is the builder for querying WorkLensWindow entities.
type WorkLensWindowQuery struct {
	config
	ctx                    *QueryContext
	order                  []worklenswindow.OrderOption
	inters                 []Interceptor
	predicates             []predicate.WorkLensWindow
	withLens               *WorkLensQuery
	withDocumentResults    *DocumentLensResultQuery
	withPullRequestResults *PullRequestLensResultQuery
	withTicketResults      *TicketLensResultQuery
	withMessageResults     *MessageLensResultQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the WorkLensWindowQuery builder.
func (_q *WorkLensWindowQuery) Where(ps ...predicate.WorkLensWindow) *WorkLensWindowQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *WorkLensWindowQuery) Limit(limit int) *WorkLensWindowQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *WorkLensWindowQuery) Offset(offset int) *WorkLensWindowQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *WorkLensWindowQuery) Unique(unique bool) *WorkLensWindowQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *WorkLensWindowQuery) Order(o ...worklenswindow.OrderOption) *WorkLensWindowQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryLens chains the current query on the "lens" edge.
func (_q *WorkLensWindowQuery) QueryLens() *WorkLensQuery {
	query := (&WorkLensClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(worklenswindow.Table, worklenswindow.FieldID, selector),
			sqlgraph.To(worklens.Table, worklens.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, worklenswindow.LensTable, worklenswindow.LensColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryDocumentResults chains the current query on the "document_results" edge.
func (_q *WorkLensWindowQuery) QueryDocumentResults() *DocumentLensResultQuery {
	query := (&DocumentLensResultClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(worklenswindow.Table, worklenswindow.FieldID, selector),
			sqlgraph.To(documentlensresult.Table, documentlensresult.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, worklenswindow.DocumentResultsTable, worklenswindow.DocumentResultsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPullRequestResults chains the current query on the "pull_request_results" edge.
func (_q *WorkLensWindowQuery) QueryPullRequestResults() *PullRequestLensResultQuery {
	query := (&PullRequestLensResultClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(worklenswindow.Table, worklenswindow.FieldID, selector),
			sqlgraph.To(pullrequestlensresult.Table, pullrequestlensresult.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, worklenswindow.PullRequestResultsTable, worklenswindow.PullRequestResultsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryTicketResults chains the current query on the "ticket_results" edge.
func (_q *WorkLensWindowQuery) QueryTicketResults() *TicketLensResultQuery {
	query := (&TicketLensResultClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(worklenswindow.Table, worklenswindow.FieldID, selector),
			sqlgraph.To(ticketlensresult.Table, ticketlensresult.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, worklenswindow.TicketResultsTable, worklenswindow.TicketResultsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryMessageResults chains the current query on the "message_results" edge.
func (_q *WorkLensWindowQuery) QueryMessageResults() *MessageLensResultQuery {
	query := (&MessageLensResultClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(worklenswindow.Table, worklenswindow.FieldID, selector),
			sqlgraph.To(messagelensresult.Table, messagelensresult.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, worklenswindow.MessageResultsTable, worklenswindow.MessageResultsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first WorkLensWindow entity from the query.
// Returns a *NotFoundError when no WorkLensWindow was found.
func (_q *WorkLensWindowQuery) First(ctx context.Context) (*WorkLensWindow, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{worklenswindow.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *WorkLensWindowQuery) FirstX(ctx context.Context) *WorkLensWindow {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first WorkLensWindow ID from the query.
// Returns a *NotFoundError when no WorkLensWindow ID was found.
func (_q *WorkLensWindowQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{worklenswindow.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *WorkLensWindowQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single WorkLensWindow entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one WorkLensWindow entity is found.
// Returns a *NotFoundError when no WorkLensWindow entities are found.
func (_q *WorkLensWindowQuery) Only(ctx context.Context) (*WorkLensWindow, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{worklenswindow.Label}
	default:
		return nil, &NotSingularError{worklenswindow.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *WorkLensWindowQuery) OnlyX(ctx context.Context) *WorkLensWindow {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only WorkLensWindow ID in the query.
// Returns a *NotSingularError when more than one WorkLensWindow ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *WorkLensWindowQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{worklenswindow.Label}
	default:
		err = &NotSingularError{worklenswindow.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *WorkLensWindowQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of WorkLensWindows.
func (_q *WorkLensWindowQuery) All(ctx context.Context) ([]*WorkLensWindow, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*WorkLensWindow, *WorkLensWindowQuery]()
	return withInterceptors[[]*WorkLensWindow](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *WorkLensWindowQuery) AllX(ctx context.Context) []*WorkLensWindow {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of WorkLensWindow IDs.
func (_q *WorkLensWindowQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(worklenswindow.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *WorkLensWindowQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *WorkLensWindowQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*WorkLensWindowQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *WorkLensWindowQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *WorkLensWindowQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *WorkLensWindowQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the WorkLensWindowQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *WorkLensWindowQuery) Clone() *WorkLensWindowQuery {
	if _q == nil {
		return nil
	}
	return &WorkLensWindowQuery{
		config:                 _q.config,
		ctx:                    _q.ctx.Clone(),
		order:                  append([]worklenswindow.OrderOption{}, _q.order...),
		inters:                 append([]Interceptor{}, _q.inters...),
		predicates:             append([]predicate.WorkLensWindow{}, _q.predicates...),
		withLens:               _q.withLens.Clone(),
		withDocumentResults:    _q.withDocumentResults.Clone(),
		withPullRequestResults: _q.withPullRequestResults.Clone(),
		withTicketResults:      _q.withTicketResults.Clone(),
		withMessageResults:     _q.withMessageResults.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithLens tells the query-builder to eager-load the nodes that are connected to
// the "lens" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *WorkLensWindowQuery) WithLens(opts ...func(*WorkLensQuery)) *WorkLensWindowQuery {
	query := (&WorkLensClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withLens = query
	return _q
}

// WithDocumentResults tells the query-builder to eager-load the nodes that are connected to
// the "document_results" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *WorkLensWindowQuery) WithDocumentResults(opts ...func(*DocumentLensResultQuery)) *WorkLensWindowQuery {
	query := (&DocumentLensResultClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withDocumentResults = query
	return _q
}

// WithPullRequestResults tells the query-builder to eager-load the nodes that are connected to
// the "pull_request_results" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *WorkLensWindowQuery) WithPullRequestResults(opts ...func(*PullRequestLensResultQuery)) *WorkLensWindowQuery {
	query := (&PullRequestLensResultClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withPullRequestResults = query
	return _q
}

// WithTicketResults tells the query-builder to eager-load the nodes that are connected to
// the "ticket_results" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *WorkLensWindowQuery) WithTicketResults(opts ...func(*TicketLensResultQuery)) *WorkLensWindowQuery {
	query := (&TicketLensResultClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withTicketResults = query
	return _q
}

// WithMessageResults tells the query-builder to eager-load the nodes that are connected to
// the "message_results" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *WorkLensWindowQuery) WithMessageResults(opts ...func(*MessageLensResultQuery)) *WorkLensWindowQuery {
	query := (&MessageLensResultClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withMessageResults = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Key string `json:"key,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.WorkLensWindow.Query().
//		GroupBy(worklenswindow.FieldKey).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *WorkLensWindowQuery) GroupBy(field string, fields ...string) *WorkLensWindowGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &WorkLensWindowGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = worklenswindow.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Key string `json:"key,omitempty"`
//	}
//
//	client.WorkLensWindow.Query().
//		Select(worklenswindow.FieldKey).
//		Scan(ctx, &v)
func (_q *WorkLensWindowQuery) Select(fields ...string) *WorkLensWindowSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &WorkLensWindowSelect{WorkLensWindowQuery: _q}
	sbuild.label = worklenswindow.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a WorkLensWindowSelect configured with the given aggregations.
func (_q *WorkLensWindowQuery) Aggregate(fns ...AggregateFunc) *WorkLensWindowSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *WorkLensWindowQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !worklenswindow.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *WorkLensWindowQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*WorkLensWindow, error) {
	var (
		nodes       = []*WorkLensWindow{}
		_spec       = _q.querySpec()
		loadedTypes = [5]bool{
			_q.withLens != nil,
			_q.withDocumentResults != nil,
			_q.withPullRequestResults != nil,
			_q.withTicketResults != nil,
			_q.withMessageResults != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*WorkLensWindow).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &WorkLensWindow{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withLens; query != nil {
		if err := _q.loadLens(ctx, query, nodes, nil,
			func(n *WorkLensWindow, e *WorkLens) { n.Edges.Lens = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withDocumentResults; query != nil {
		if err := _q.loadDocumentResults(ctx, query, nodes,
			func(n *WorkLensWindow) { n.Edges.DocumentResults = []*DocumentLensResult{} },
			func(n *WorkLensWindow, e *DocumentLensResult) {
				n.Edges.DocumentResults = append(n.Edges.DocumentResults, e)
			}); err != nil {
			return nil, err
		}
	}
	if query := _q.withPullRequestResults; query != nil {
		if err := _q.loadPullRequestResults(ctx, query, nodes,
			func(n *WorkLensWindow) { n.Edges.PullRequestResults = []*PullRequestLensResult{} },
			func(n *WorkLensWindow, e *PullRequestLensResult) {
				n.Edges.PullRequestResults = append(n.Edges.PullRequestResults, e)
			}); err != nil {
			return nil, err
		}
	}
	if query := _q.withTicketResults; query != nil {
		if err := _q.loadTicketResults(ctx, query, nodes,
			func(n *WorkLensWindow) { n.Edges.TicketResults = []*TicketLensResult{} },
			func(n *WorkLensWindow, e *TicketLensResult) { n.Edges.TicketResults = append(n.Edges.TicketResults, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withMessageResults; query != nil {
		if err := _q.loadMessageResults(ctx, query, nodes,
			func(n *WorkLensWindow) { n.Edges.MessageResults = []*MessageLensResult{} },
			func(n *WorkLensWindow, e *MessageLensResult) {
				n.Edges.MessageResults = append(n.Edges.MessageResults, e)
			}); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *WorkLensWindowQuery) loadLens(ctx context.Context, query *WorkLensQuery, nodes []*WorkLensWindow, init func(*WorkLensWindow), assign func(*WorkLensWindow, *WorkLens)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*WorkLensWindow)
	for i := range nodes {
		fk := nodes[i].WorkLensID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(worklens.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "work_lens_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *WorkLensWindowQuery) loadDocumentResults(ctx context.Context, query *DocumentLensResultQuery, nodes []*WorkLensWindow, init func(*WorkLensWindow), assign func(*WorkLensWindow, *DocumentLensResult)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*WorkLensWindow)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(documentlensresult.FieldWorkLensWindowID)
	}
	query.Where(predicate.DocumentLensResult(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(worklenswindow.DocumentResultsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.WorkLensWindowID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "work_lens_window_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *WorkLensWindowQuery) loadPullRequestResults(ctx context.Context, query *PullRequestLensResultQuery, nodes []*WorkLensWindow, init func(*WorkLensWindow), assign func(*WorkLensWindow, *PullRequestLensResult)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*WorkLensWindow)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(pullrequestlensresult.FieldWorkLensWindowID)
	}
	query.Where(predicate.PullRequestLensResult(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(worklenswindow.PullRequestResultsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.WorkLensWindowID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "work_lens_window_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *WorkLensWindowQuery) loadTicketResults(ctx context.Context, query *TicketLensResultQuery, nodes []*WorkLensWindow, init func(*WorkLensWindow), assign func(*WorkLensWindow, *TicketLensResult)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*WorkLensWindow)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(ticketlensresult.FieldWorkLensWindowID)
	}
	query.Where(predicate.TicketLensResult(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(worklenswindow.TicketResultsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.WorkLensWindowID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "work_lens_window_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *WorkLensWindowQuery) loadMessageResults(ctx context.Context, query *MessageLensResultQuery, nodes []*WorkLensWindow, init func(*WorkLensWindow), assign func(*WorkLensWindow, *MessageLensResult)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*WorkLensWindow)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(messagelensresult.FieldWorkLensWindowID)
	}
	query.Where(predicate.MessageLensResult(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(worklenswindow.MessageResultsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.WorkLensWindowID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "work_lens_window_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *WorkLensWindowQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *WorkLensWindowQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(worklenswindow.Table, worklenswindow.Columns, sqlgraph.NewFieldSpec(worklenswindow.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, worklenswindow.FieldID)
		for i := range fields {
			if fields[i] != worklenswindow.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withLens != nil {
			_spec.Node.AddColumnOnce(worklenswindow.FieldWorkLensID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *WorkLensWindowQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(worklenswindow.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = worklenswindow.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// WorkLensWindowGroupBy is the group-by builder for WorkLensWindow entities.
type WorkLensWindowGroupBy struct {
	selector
	build *WorkLensWindowQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *WorkLensWindowGroupBy) Aggregate(fns ...AggregateFunc) *WorkLensWindowGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *WorkLensWindowGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WorkLensWindowQuery, *WorkLensWindowGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *WorkLensWindowGroupBy) sqlScan(ctx context.Context, root *WorkLensWindowQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// WorkLensWindowSelect is the builder for selecting fields of WorkLensWindow entities.
type WorkLensWindowSelect struct {
	*WorkLensWindowQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *WorkLensWindowSelect) Aggregate(fns ...AggregateFunc) *WorkLensWindowSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *WorkLensWindowSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WorkLensWindowQuery, *WorkLensWindowSelect](ctx, _s.WorkLensWindowQuery, _s, _s.inters, v)
}

func (_s *WorkLensWindowSelect) sqlScan(ctx context.Context, root *WorkLensWindowQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
